<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cc.order.mapper.OrderItemMapper">

    <resultMap id="OrderItemResultMap" type="com.cc.order.model.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="sku_id" property="skuId"/>
        <result column="sku_code" property="skuCode"/>
        <result column="sku_attributes" property="skuAttributes"/>
        <result column="price" property="price"/>
        <result column="quantity" property="quantity"/>
        <result column="total_price" property="totalPrice"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findById" resultMap="OrderItemResultMap">
        SELECT * FROM order_items WHERE id = #{id}
    </select>

    <select id="findByOrderId" resultMap="OrderItemResultMap">
        SELECT * FROM order_items WHERE order_id = #{orderId} ORDER BY id
    </select>

    <insert id="insert" parameterType="com.cc.order.model.OrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_items (order_id, product_id, product_name, product_image, sku_id, sku_code,
                              sku_attributes, price, quantity, total_price)
        VALUES (#{orderId}, #{productId}, #{productName}, #{productImage}, #{skuId}, #{skuCode},
                #{skuAttributes}, #{price}, #{quantity}, #{totalPrice})
    </insert>

    <select id="countByOrderId" resultType="int">
        SELECT COUNT(*) FROM order_items WHERE order_id = #{orderId}
    </select>

</mapper>