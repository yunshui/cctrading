spring:
  datasource:
    url: jdbc:mysql://mysql:3306/ccorder?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
  redis:
    host: redis
    port: 6379
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848

logging:
  level:
    com.cc.order: info